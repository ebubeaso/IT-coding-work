// This TypeScript file will have interfaces to add alongside our application
export interface AsoInterface {
    fullName: string,
    age: number,
    favColor: string,
    favFood: string,
    likes: string[] | null,
    dislikes: string[] | null,
    passions: string[]
}