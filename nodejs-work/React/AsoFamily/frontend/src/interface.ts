export interface AsoStats {
    name: string,
    fullName: string,
    age: number,
    favoriteColor: string,
    favoriteFood: string | string[],
    likes: string[]
    dislikes: string[]
    passions: string[]
}